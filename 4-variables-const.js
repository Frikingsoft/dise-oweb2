/* Existen 3 formas de crear variables 
    var   --- No usar --- Las variables con var son globales
    let
    const --- una vez que se asigna un valor a una variable constante, 
    no se puede reasignar ese valor más adelante en el código.
*/
const numero = 12

// numero = 15  Esto no se puede hacer

console.log(numero)